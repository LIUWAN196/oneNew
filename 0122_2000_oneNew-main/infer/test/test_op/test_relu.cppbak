#include "../host/op/relu.h"

int main()
{

    op *op_1 = new relu();

    BUFFER_GROUP_S *a;
    BUFFER_GROUP_S *b;
    BUFFER_GROUP_S *c;

    std::string op_name = "relu6";
    op_1->prepare(op_name);

    op_1->forward(a, b, c);

    return 0;
}